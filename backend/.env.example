# ===== DATABASE =====
# PostgreSQL Connection String (Neon, Railway, Supabase, etc.)
# Format: postgresql://user:password@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://user:password@host:5432/groupietracker?sslmode=require

# ===== STRIPE =====
# Secret Key (Dashboard Stripe → Developers → API Keys → Secret key)
# Format: sk_test_XXXXXXXXXX (test) ou sk_live_XXXXXXXXXX (production)
STRIPE_SECRET_KEY=sk_test_VOTRE_CLE_SECRETE

# Webhook Secret (Dashboard Stripe → Developers → Webhooks → Signing secret)
# Format: whsec_XXXXXXXXXX
STRIPE_WEBHOOK_SECRET=whsec_VOTRE_WEBHOOK_SECRET

# ===== AUTH =====
# Secret JWT pour signer les tokens (générer avec: openssl rand -base64 32)
# Doit être très aléatoire, minimum 32 caractères
JWT_SECRET=votre-secret-jwt-256-bits-tres-aleatoire-changez-moi

# ===== OAUTH GOOGLE =====
# Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=123456-xxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-votre-secret-google
GOOGLE_REDIRECT_URL=http://localhost:8080/api/auth/google/callback

# ===== EMAIL (SendGrid) =====
# SendGrid Dashboard → Settings → API Keys
# Format: SG.XXXXXXXXXX
SENDGRID_API_KEY=SG.votre-cle-sendgrid
FROM_EMAIL=noreply@groupietracker.fr

# ===== IA (OpenAI) =====
# OpenAI Platform → API Keys
# Format: sk-proj-XXXXXXXXXX
OPENAI_API_KEY=sk-proj-votre-cle-openai

# ===== STORAGE S3 (MinIO ou AWS S3) =====
# Si MinIO local: localhost:9000
# Si AWS S3: s3.amazonaws.com
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_USE_SSL=false
MINIO_BUCKET=groupietracker

# ===== MONITORING (Sentry) =====
# Sentry.io → Settings → Projects → Client Keys (DSN)
# Format: https://xxx@yyy.ingest.sentry.io/zzz
SENTRY_DSN=https://VOTRE_SENTRY_DSN

# ===== CORS =====
# Liste des origines autorisées (séparées par des virgules)
# En dev: http://localhost:5173
# En prod: https://votre-frontend.vercel.app
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ===== SERVER =====
# Port du serveur (Render utilise la variable PORT automatiquement)
PORT=8080
